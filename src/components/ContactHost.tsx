// components/ContactHost.tsx
import React from "react";

const ContactHost = () => {
  return (
    <div className="contact-host">
      <h2 className="credentialheading">Send Message</h2>
      <button>Contact host</button>
    </div>
  );
};

export default ContactHost;
